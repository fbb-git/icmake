#include "builtin.ih"

void fun_trimleft()
{
    reg = *stringTrimLeft(top());

//    trimFront(stringStr(top()));
//
//    char *cp = stringStr(top());
//
//    while (isspace(*cp))
//      ++cp;
//
//    reg = *stringConstructor_cP(cp);
}
