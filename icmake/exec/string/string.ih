#include "string.h"

#include <stdlib.h>
#include <string.h>
#include <ctype.h>

#include "../var/var.h"
#include "../virtual/virtual.h"
#include "../int/int.h"

TextData *s_new(char *cp);    /* cp must be free-able */

inline char *s_str(String const *arg)
{
    return arg->data->str;
}
