#include "process.h"

#include <string.h>
#include <unistd.h>

#include "../../rss/rss.h"
#include "../util/util.h"

void useAll(Process *process);
void inspectUseAll(Process *process, int idx);
char const *allPath(Process *process, int idx);

void touch(Process *process, char const *path);

void unlinkGch(Process *process, int idx);
void rmGch(Process *process);
void pInspectGch(Process *process, int idx);


