var_expr:
    enterid
    zeroframe                           /* no explicit initialization */
|
    enter_varid
    '='
    expression
    {
        $$ = *expr_stmnt(assign(&$1, &$3));    /* explicit initialization */
    }        
;

