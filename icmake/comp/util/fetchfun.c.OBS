#include "util.ih"

size_t fetchfun()
{
    register size_t idx;
                                            /* test function defined */
    if ((idx = looksym(&g_funtab)) == g_funtab.n_defined)
        util_semantic("Function '%s()' not defined", gu_lexstring);

    return idx;
}
