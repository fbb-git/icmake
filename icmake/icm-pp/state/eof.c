#include "state.ih"

void state_eof(size_t linenr)
{
//    if (st_size != 1)
//        rss_fatal(filestack_tos()->filename, linenr, 
//                "%u #if(n)def(s) without matching #endif encountered",
//                  st_size - 1);
}











