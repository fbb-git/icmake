#include "process.ih"

void inspectDepending(Process *process, int idx, int *indicator, int size)
{
//    int *inspect = depending(idx);    // get depending
                                                    // directories 
//
//    for (int depIdx = 0; depIdx != size; ++depIdx)  // visit all dep. dirs.
//    {
//        if (indicator[depIdx] || inspect[depIdx] == 0)  // been here, or not
//            continue;                                   // depending: cont.
//
//        inspect[depIdx] = 1;                        // visited dep. dir.
//        touch(process, depIdx);                     // touch the USE_ALL file
//    }
}
