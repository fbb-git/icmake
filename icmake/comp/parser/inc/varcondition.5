var_condition:
    enter_varid
    {
        p_generateCode(&$1, op_push_imm, 0);
        $$ = $1;
    }
|
    enter_varid
    '='
    expression
    {
        $$ = *p_assign(&$1, &$3);    /* explicit initialization */
    }        
;
