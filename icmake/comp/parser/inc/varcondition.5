var_condition:
    enter_varid
    {
        gencode(&$1, op_push_imm, 0);
        $$ = $1;
    }
|
    enter_varid
    '='
    expression
    {
        $$ = *assign(&$1, &$3);    /* explicit initialization */
    }        
;
